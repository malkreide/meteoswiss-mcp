---
id: OPS-002
title: "Doku-Standard: bilingualer README, ASCII-Diagramm, Limits-Sektion"
category: OPS
severity: medium
applies_when: 'always'
pdf_ref: "Anhang C2"
evidence_required: 4
---

# OPS-002 — Doku-Standard

## Description

ARCH-011 verlangt die Existenz von `README.md`, `README.de.md`, `CHANGELOG.md` als Files. OPS-002 verlangt **Inhalt-Disziplin** für diese Files — nicht nur Existenz.

Pro Server-README muss enthalten sein:

| Sektion | Pflicht | Zweck |
|---|---|---|
| Anchor-Demo-Query | ✅ | Konkrete natürlich-sprachliche Frage, die der Server beantworten kann (z.B. «Welche Vorstösse zu KI in der Schule sind hängig?») |
| Installation | ✅ | `uvx`, `npx` oder Docker-Run-Commando, copy-paste-fähig |
| Tools-Übersicht | ✅ | Liste aller exponierter Tools mit kurzer Beschreibung |
| Konfiguration | ✅ | Env-Vars, Config-Files, Secret-Anforderungen |
| Sicherheits-Hinweise | ✅ | Datenklasse, Auth-Modell, Trifecta-Bewertung (Synergie zu SEC-019) |
| ASCII-Architekturdiagramm | ✅ | Eine Bildschirm-grosse Visualisierung der Datenflüsse |
| Bekannte Limits | ✅ | Was tut der Server **nicht**? Was sind Edge-Cases? |
| Kontribution / Lizenz | ✅ | CONTRIBUTING.md-Verweis, Lizenz-Hinweis |

Plus die deutsche README, die mindestens die Top-Level-Sektionen synchron hält (siehe ARCH-011).

Plus ein **CHANGELOG.md** im Keep-a-Changelog-Format mit:
- Pro Release ein Datum
- Sektionen `Added`, `Changed`, `Deprecated`, `Removed`, `Fixed`, `Security`
- Spec-Version-Bumps explizit (Synergie zu ARCH-012)

Die Anchor-Demo-Query ist der wichtigste Punkt — sie ist der einzige Teil des READMEs, den ein nicht-technischer Stakeholder (GL, Pädagogik, Datenschutzbeauftragte:r) auch wirklich liest. Sie macht abstrakte Tool-Listen anschaulich.

## Verification

### Modus 1: documentation_check (Pflicht-Sektionen vorhanden)

```bash
# Pro README prüfen, ob alle Pflicht-Sektionen da sind
for readme in README.md README.de.md; do
  test -f "$readme" || { echo "✗ $readme missing"; continue; }
  echo "=== $readme ==="
  grep -cE '^## ' "$readme"
  for section in 'Demo' 'Anchor' 'Installation' 'Tools' 'Konfiguration\|Configuration' 'Sicherheit\|Security' 'Architektur\|Architecture' 'Limits\|Limit' 'Lizenz\|License'; do
    grep -qiE "$section" "$readme" && echo "✓ $section" || echo "✗ $section"
  done
done
```

### Modus 2: code_review (Anchor-Demo-Query vorhanden)

```bash
grep -B 2 -A 5 -iE 'demo.query|anchor|beispiel.frage|example.question' README.md README.de.md
```

**Pass-Pattern:**

```markdown
## Anchor Demo Query

> «Welche parlamentarischen Vorstösse zum Thema Künstliche Intelligenz im
> Schulkontext sind aktuell hängig?»

Der Server beantwortet diese Frage durch:
1. `parlament_ch__search_motions(query="KI Schule", status="submitted")`
2. Filterung auf `business_type="motion"` und Status-Set
3. Rückgabe als strukturierte Liste mit Title, Submitter, Datum, URL
```

### Modus 3: code_review (ASCII-Architekturdiagramm)

```bash
# ASCII-Boxen, Pfeile, ASCII-Tree-Pattern
grep -B 1 -A 20 -E '^```$|^```text|^```mermaid' README.md | grep -E '─|│|┌|└|═|║|->|=>' | head -10
```

**Pass-Pattern:**

```markdown
## Architektur

```
┌──────────────────┐    stdio/SSE    ┌─────────────────────┐
│   Claude         │ ◄─────────────► │   parlament-mcp     │
│   Desktop / Code │                 │   (FastMCP)         │
└──────────────────┘                 └──────────┬──────────┘
                                                │ HTTPS
                                                ▼
                                     ┌─────────────────────┐
                                     │   Curia Vista API   │
                                     │   (parlament.ch)    │
                                     └─────────────────────┘
```

Datenfluss:
1. LLM ruft `search_motions()` auf
2. Server validiert Args (Pydantic), bildet OData-Query
3. HTTPS-Request an Curia Vista, Antwort transformiert zu MCP-Response
4. Response geht zurück an LLM-Kontext
```

### Modus 4: documentation_check (Limits-Sektion vorhanden)

```bash
grep -B 1 -A 10 -iE '^## (Bekannte )?Limits?|^## Known [Ll]imitations' README.md README.de.md
```

**Pass-Pattern:**

```markdown
## Bekannte Limits

- **Sprache:** Server gibt Daten primär auf Deutsch zurück (Curia Vista
  Standard). Französische und italienische Übersetzungen via
  `language="fr"` / `"it"`-Parameter, nicht aller Felder verfügbar.
- **Aktualität:** Curia Vista hat ca. 5 Minuten Verzögerung gegenüber Live-Sessions
  des Parlaments. Kein Echtzeit-Streaming.
- **Historische Daten:** Nur Geschäfte seit 2002 vollständig erfasst.
- **Volltext-Suche:** OData-Backend unterstützt nur `substringof()`-Pattern,
  keine Fuzzy-Search oder Stemming.
- **Rate-Limit:** Curia Vista limitiert auf ~60 Requests/Minute. Server
  cached aggressiv (TTL 1h für Search-Results).
```

## Pass Criteria

- [ ] `README.md` enthält alle 8 Pflicht-Sektionen (Anchor-Demo, Installation, Tools, Config, Security, Architektur-Diagramm, Limits, Lizenz)
- [ ] `README.de.md` enthält die gleichen Top-Level-Sektionen
- [ ] Anchor-Demo-Query ist konkret und natürlich-sprachlich
- [ ] ASCII-Architekturdiagramm ist vorhanden (oder Mermaid-Diagramm als gleichwertig)
- [ ] Bekannte-Limits-Sektion ist explizit (mindestens 3 Limits genannt)
- [ ] CHANGELOG.md im Keep-a-Changelog-Format (Synergie zu ARCH-012)
- [ ] CONTRIBUTING.md vorhanden, bilingual

## Common Failures

| Anti-Pattern | Risiko |
|---|---|
| Generischer README ohne Anchor-Demo | Stakeholder verstehen den Use-Case nicht |
| Tools-Liste ohne Beispiele | Onboarding-Last für neue Maintainer |
| Limits-Sektion fehlt | User stossen auf Edge-Cases ohne Vorwarnung |
| README.de.md veraltet gegenüber README.md | Mehrsprachigkeit nominell, nicht real |
| Architektur als Fliesstext statt Diagramm | Datenfluss schwer erfassbar |

## Remediation

### Schritt 1: README-Skelett

Vorlage in `templates/server-readme.md` (im Skill-Repo). Pro Server kopieren und befüllen.

### Schritt 2: Anchor-Demo-Query identifizieren

Für jeden Server eine konkrete Frage formulieren, die ein:e Schulamts-Mitarbeiter:in stellen würde. Diese im README prominent.

### Schritt 3: Limits-Workshop

15-Minuten-Workshop pro Server mit Maintainer. Drei Fragen:
- Was funktioniert sehr gut?
- Wo bricht der Server typischerweise?
- Welche Erwartungen sollte der User **nicht** haben?

Resultat als Limits-Sektion.

### Schritt 4: ASCII-Diagramm

Tools wie [Mermaid](https://mermaid.js.org/) oder einfach pure ASCII mit `─`-Box-Drawing-Characters. Bei mehr als 5 Komponenten: zwei Diagramme (Übersicht + Detail).

### Schritt 5: README.de.md synchron halten

Bei jedem PR mit README-Änderungen muss README.de.md auch aktualisiert werden. CI-Hook:

```yaml
- name: Check README parity
  run: |
    grep -E '^## ' README.md | sort > /tmp/en.txt
    grep -E '^## ' README.de.md | sort > /tmp/de.txt
    diff -u /tmp/en.txt /tmp/de.txt || (echo "README sections out of sync"; exit 1)
```

## Effort

M — 1–3 Tage pro Server für Initial-Doku. Bei portfolio-weitem Roll-out: ~1 Tag pro Server.

## References

- Anhang C2 — Dokumentation als Best Practice
- ARCH-011 — Repo-Struktur (Voraussetzung für File-Existenz)
- ARCH-012 — Spec-Versionierung (Synergie zu CHANGELOG)
- SEC-019 — Lethal Trifecta (Synergie zu Security-Hinweisen-Sektion)
- [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/)
