---
id: SEC-022
title: "Tool-Hash-Pinning + Namespace-Präfix gegen Rug Pull"
category: SEC
severity: high
applies_when: 'enterprise_context == true or stadt_zuerich_context == true or is_cloud_deployed == true'
pdf_ref: "Anhang B4"
evidence_required: 3
---

# SEC-022 — Tool-Hash-Pinning gegen Rug Pull

## Description

SEC-015 deckt **Tool-Poisoning** ab — bösartige Inhalte in Tool-Beschreibungen beim Onboarding. SEC-022 ergänzt das um zwei verwandte Angriffsklassen:

**Rug Pull:** Server registriert beim Onboarding harmlose Tool-Beschreibungen. User stimmt zu. Nach erfolgreicher Approval ändert der Server seine Tool-Beschreibungen — z.B. fügt versteckte Instruktionen hinzu, die der LLM beim nächsten Aufruf befolgt. Klassischer Bait-and-Switch.

**Cross-Server Tool Shadowing:** Ein bösartiger Server registriert ein Tool mit demselben Namen wie ein vertrauenswürdiger Server (z.B. beide haben `search_documents`). Der LLM-Host kann sie nicht unterscheiden. Wenn der Bösartige zuerst antwortet, wird sein Tool aufgerufen statt das vertrauenswürdige.

Mitigation:

**1. Tool-Hash-Pinning:** Beim ersten Onboarding speichert der Host SHA-256-Hashes aller Tool-Definitionen (Name + Beschreibung + Schema). Bei jeder folgenden `tools/list`-Antwort wird gegen die gespeicherten Hashes verglichen. Änderung → User-Bestätigung erforderlich. Server-Maintainer-Pflicht: Hashes vor Release neu berechnen, in CHANGELOG dokumentieren.

**2. Namespace-Präfix:** Alle Tool-Namen werden mit der Server-Identität präfixiert: `acme_finance__create_invoice` statt `create_invoice`. Verhindert Shadowing — zwei Server können nicht denselben Tool-Namen produzieren.

**3. Server-Signaturen:** Bei kompatiblen Hosts (zukünftig): signiertes Server-Manifest mit kryptographischem Beweis der Server-Identität.

Im Schulamt-Kontext heute primär relevant durch Namespace-Präfixe — bei Multi-Server-Gateway-Setups (siehe SCALE-005) muss Shadowing strukturell unmöglich sein.

## Verification

### Modus 1: code_review (Namespace-Präfix konsistent)

```bash
grep -rE '@mcp\.tool|registerTool|tools\.add' src/ -A 3 | grep -E 'name\s*=' | head -30
```

**Pass-Pattern:**

```python
SERVER_NAMESPACE = "schulamt_zh"  # oder "zh_education", "zurich_opendata", etc.

@mcp.tool(name=f"{SERVER_NAMESPACE}__search_motions")
async def search_motions(args, ctx):
    ...

@mcp.tool(name=f"{SERVER_NAMESPACE}__get_school_profile")
async def get_school_profile(args, ctx):
    ...
```

**Akzeptabel-Pattern (FastMCP-Auto-Namespace):**

```python
# FastMCP setzt name automatisch aus dem Funktionsnamen.
# Konsistente Präfixierung auf Funktions-Ebene:
@mcp.tool()
async def schulamt_zh__search_motions(args, ctx):
    ...
```

**Fail-Pattern:**

```python
# FAIL: generischer Name, kollisionsanfällig
@mcp.tool()
async def search(args, ctx):
    ...

@mcp.tool()
async def get_data(args, ctx):
    ...
```

### Modus 2: code_review (Tool-Definitions-Hash im CI)

Bei jedem Release sollte ein Hash-Snapshot der Tool-Definitionen mitkommen. Das ist nicht im Server-Code selbst — sondern im Release-Workflow.

```bash
ls .github/workflows/ | xargs grep -l 'hash\|sha256\|tool.snapshot' 2>/dev/null
```

**Pass-Pattern (`.github/workflows/release.yml`):**

```yaml
- name: Generate tool definition hashes
  run: |
    python -c "
    import json, hashlib
    from server import mcp
    snapshot = {}
    for name, tool in sorted(mcp.tools.items()):
        canonical = json.dumps({
            'name': tool.name,
            'description': tool.description,
            'input_schema': tool.input_schema.model_json_schema(),
        }, sort_keys=True)
        snapshot[name] = hashlib.sha256(canonical.encode()).hexdigest()
    with open('tool-hashes.json', 'w') as f:
        json.dump(snapshot, f, indent=2)
    "

- name: Commit hash file with release
  run: |
    git add tool-hashes.json
    git commit -m "Tool definition hashes for ${{ github.ref_name }}"
```

### Modus 3: documentation_check (CHANGELOG nennt Tool-Definition-Änderungen)

```bash
grep -iE 'tool.*description|tool.*added|tool.*changed|tool.*hash' CHANGELOG.md
```

**Pass-Pattern:**

```markdown
## [0.4.0] — 2026-04-15
### Changed
- Tool description for `schulamt_zh__search_motions` clarified scope
  (parlamentarische Motionen, nicht Initiativen). Hash changed from
  `a1b2...` to `c3d4...`. Re-approval needed in Claude Desktop.

### Added
- New tool `schulamt_zh__aggregate_by_district` (hash `e5f6...`)
```

## Pass Criteria

- [ ] Alle Tools haben Namespace-Präfix mit Server-Identität (`<server>__<tool>`)
- [ ] Server-Identity-Präfix ist konsistent über alle Tools (FrozenSet, nicht config-mutierbar)
- [ ] Bei Releases: Hash-Snapshot der Tool-Definitionen wird erzeugt und im Repo gespeichert
- [ ] CHANGELOG-Einträge nennen Tool-Definition-Änderungen explizit (Synergie zu ARCH-012)
- [ ] Bei Tool-Description-Änderungen: User-Re-Approval-Hinweis im CHANGELOG
- [ ] Bei breakenden Tool-Changes: Major-Version-Bump (semver)

## Common Failures

| Anti-Pattern | Risiko |
|---|---|
| Generische Tool-Namen ohne Namespace | Shadowing zwischen Servern möglich |
| Inkonsistente Präfixe (manche Tools mit, manche ohne) | Inconsistent Tool-Manifest |
| Keine Hash-Snapshot bei Release | Rug-Pull-Detection nicht möglich |
| Tool-Description-Änderung ohne CHANGELOG-Eintrag | User-Re-Approval-Notwendigkeit nicht klar |
| Kollisionsfähige Namespaces (zwei Server `data_*`) | Wenig Schutz gegenüber Shadowing |

## Remediation

### Schritt 1: Namespace-Audit

Server-Identity festlegen — typisch der Repo-Name als snake_case-Präfix:

| Repo | Namespace |
|---|---|
| `zh-education-mcp` | `zh_education` |
| `zurich-opendata-mcp` | `zurich_opendata` |
| `parlament-mcp` | `parlament_ch` |

### Schritt 2: Tool-Renaming

```diff
- @mcp.tool()
- async def search(query: str): ...
+ @mcp.tool(name="zh_education__search")
+ async def search(query: str): ...
```

Bei Renaming: Major-Version-Bump, da Tool-Namen Breaking-Changes sind.

### Schritt 3: Hash-Snapshot-Workflow

CI-Step wie im Pass-Pattern Modus 2. `tool-hashes.json` als Artefakt im Release.

### Schritt 4: Bei Update-Disziplin (Synergie zu ARCH-012)

CHANGELOG-Template um «Tool Definition Changes»-Sektion erweitern:

```markdown
## [Unreleased]
### Tool Definition Changes
- (none)
```

## Effort

M — 1–3 Tage pro Server. Namespace-Renaming + Hash-Workflow + CHANGELOG-Updates.

## References

- Anhang B4 — Tool Poisoning und Rug Pull verhindern
- SEC-015 — Pre-Flight Tool-Poisoning Detection (komplementär)
- ARCH-012 — Spec-Versionierung (Synergie via CHANGELOG)
- SCALE-005 — MCP-Gateway (Voraussetzung bei Multi-Server)
