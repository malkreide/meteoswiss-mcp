---
id: SEC-018
title: "Input-Validation an Tool-Boundaries (Pydantic strict / Zod)"
category: SEC
severity: high
applies_when: 'always'
pdf_ref: "Sec 3 / Sec 4 (Defense-in-Depth)"
evidence_required: 3
---

# SEC-018 — Input-Validation an Tool-Boundaries

## Description

Tool-Argumente kommen vom LLM — einer probabilistischen Quelle, die halluzinieren, formattieren-falsch oder von Prompt-Injection beeinflusst sein kann. Ohne strikte Input-Validation am Tool-Boundary werden invalide oder bösartige Inputs in die Geschäftslogik weitergereicht und können dort:

1. **Unerwartete Exceptions** auslösen → Error-Pfad könnte Information leaken (siehe OBS-002)
2. **Type Confusion** triggern → z.B. `user_id: int` aber LLM schickt String → SQL-Coercion-Bug
3. **Range-Violations** verursachen → z.B. negative Pagination-Limits → DB-Crash oder Memory-Explosion
4. **Encoding-Probleme** propagieren → Unicode-Tricks, Null-Bytes, Control-Characters

Dies ist die universelle Defense-in-Depth-Schicht, die unabhängig von SDK-005 (TypeScript/Zod) für **alle** SDKs gilt: jedes Tool-Argument wird gegen ein striktes Schema geprüft, **bevor** es in Tool-Logik fliesst.

Im Python-Ökosystem ist Pydantic v2 mit `strict=True` der Standard. Im TypeScript-Ökosystem Zod (siehe SDK-005). In Go: validator.v10 oder go-playground/validator. In Rust: serde + validator.

## Verification

### Modus 1: code_review (Schema-Coverage)

```bash
# Python: alle Tool-Funktionen haben typisierte Parameter mit Pydantic-Constraints
grep -rB2 -A10 '@mcp\.tool' src/ | grep -E "Field\(|conint|constr|StringConstraints"

# TypeScript: alle registerTool-Calls haben z.object()-Schema
grep -rE "registerTool|tools\.add" src/ -A10 | grep "z\.object"
```

**Pass-Pattern (Python / Pydantic v2):**

```python
from typing import Annotated
from pydantic import Field, BaseModel, StringConstraints

class SearchArgs(BaseModel):
    """Schema für search-Tool. Strict mode rejects extra fields."""
    model_config = {"strict": True, "extra": "forbid"}

    query: Annotated[str, StringConstraints(
        min_length=2,
        max_length=200,
        pattern=r"^[\w\säöüÄÖÜß\-.,?!]+$",  # Whitelist statt Blacklist
    )]
    limit: Annotated[int, Field(ge=1, le=100)] = 10
    include_archived: bool = False

@mcp.tool()
async def search(args: SearchArgs, ctx: Context) -> dict:
    # args ist garantiert valide — kein weiteres Defensive-Coding nötig
    return await db.search(args.query, limit=args.limit)
```

**Fail-Pattern:**

```python
@mcp.tool()
async def search(query: str, limit: int = 10):
    # Kein min/max für limit → LLM kann limit=10000 schicken
    # Kein Pattern für query → Injection / Encoding-Tricks möglich
    # Kein extra=forbid → unbekannte Felder werden stillschweigend akzeptiert
    return await db.search(query, limit=limit)
```

### Modus 2: code_review (Strict Mode aktiviert)

```bash
grep -rE 'strict.*=.*True|strict:\s*true|forbid.*extra' src/
```

Bei Pydantic v2: `model_config = {"strict": True, "extra": "forbid"}` muss explizit gesetzt sein. Ohne das macht Pydantic Coercion (`"1"` → `1`), was subtile Bugs erzeugt.

### Modus 3: runtime_test (Invalid-Input wird abgewiesen)

```bash
# Limit ausserhalb Range
curl -X POST https://server/mcp/call -d '{
  "jsonrpc":"2.0","id":1,"method":"tools/call","params":{
    "name":"search","arguments":{"query":"test","limit":99999}
  }
}'
# Erwartet: isError mit "Validation error: limit must be ≤ 100"

# Query mit Control-Characters
curl -X POST https://server/mcp/call -d '{
  ...,"arguments":{"query":"test\u0000injection"}
}'
# Erwartet: isError, kein Crash, kein Pass-Through
```

### Modus 4: code_review (Whitelist statt Blacklist)

Pattern müssen Whitelist-basiert sein (`^[allowed]+$`), nicht Blacklist (`^(?!.*evil)`). Letzteres lässt sich mit Unicode-Normalisierung, Encoding oder neuen Tricks umgehen.

## Pass Criteria

- [ ] **Alle** Tool-Argumente haben Schema-Validation (Pydantic / Zod / vergleichbar)
- [ ] Numerische Felder haben `ge`/`le`-Constraints (kein unbegrenzter Range)
- [ ] String-Felder haben `min_length`/`max_length` und idealerweise `pattern`
- [ ] Pattern sind Whitelist-basiert, nicht Blacklist
- [ ] Bei Pydantic: `strict=True` und `extra="forbid"` explizit gesetzt
- [ ] Validation-Errors landen als `isError` im Tool-Result, nicht als Server-Crash (siehe OBS-001)
- [ ] Tests decken Edge-Cases ab: zu lange Strings, Out-of-Range-Numbers, unbekannte Felder

## Common Failures

| Anti-Pattern | Risiko |
|---|---|
| Argumente als plain `str`, `int` ohne Constraints | LLM-Halluzinationen erzeugen Crash oder Resource-Exhaustion |
| Pydantic ohne `strict=True` | `"true"` wird zu `True` gecoerct, subtle Bugs |
| `extra="allow"` (Default) | Unbekannte Felder werden stillschweigend ignoriert — Prompt-Injection-Surface |
| Blacklist-Pattern (`^(?!.*\\.\\.)`) | Umgehbar via Encoding (`%2E%2E`) oder Unicode |
| Validation in Tool-Body statt im Schema | Inkonsistent, oft vergessen |
| `try/except: pass` um die Validation | Validation-Bypass |

## Remediation

### Schritt 1: Schema pro Tool extrahieren

```diff
+ from typing import Annotated
+ from pydantic import BaseModel, Field, StringConstraints
+
+ class SearchArgs(BaseModel):
+     model_config = {"strict": True, "extra": "forbid"}
+     query: Annotated[str, StringConstraints(min_length=2, max_length=200)]
+     limit: Annotated[int, Field(ge=1, le=100)] = 10

  @mcp.tool()
- async def search(query: str, limit: int = 10) -> dict:
+ async def search(args: SearchArgs, ctx: Context) -> dict:
-     return await db.search(query, limit=limit)
+     return await db.search(args.query, limit=args.limit)
```

### Schritt 2: ValidationError sauber behandeln

```python
from pydantic import ValidationError

@mcp.tool()
async def search(args: SearchArgs, ctx: Context) -> dict:
    try:
        # Pydantic validiert beim Parsing automatisch — kein Aufruf nötig
        # Falls manuell aus dict gebaut: SearchArgs.model_validate(raw_dict)
        return await db.search(args.query, limit=args.limit)
    except ValidationError as e:
        # Wird normal nicht erreicht (FastMCP fängt das ab),
        # aber Defense-in-Depth:
        return {
            "isError": True,
            "content": [TextContent(
                type="text",
                text=f"Invalid arguments: {e.errors()[0]['msg']}"
            )],
        }
```

### Schritt 3: Tests gegen Edge-Cases

```python
@pytest.mark.parametrize("invalid_args,expected_error", [
    ({"query": "a", "limit": 10}, "min_length"),       # zu kurz
    ({"query": "x"*500, "limit": 10}, "max_length"),   # zu lang
    ({"query": "test", "limit": 0}, "greater_than_or_equal"),
    ({"query": "test", "limit": 99999}, "less_than_or_equal"),
    ({"query": "test", "limit": 10, "evil": "field"}, "extra_forbidden"),
])
async def test_search_rejects_invalid(invalid_args, expected_error):
    with pytest.raises(ValidationError) as exc:
        SearchArgs.model_validate(invalid_args)
    assert any(expected_error in err["type"] for err in exc.value.errors())
```

## Effort

S — < 1 Tag pro Server bei wenigen Tools, M bei vielen Tools (10+).

## References

- PDF Sec 3 — SDK-Standards
- PDF Sec 4 — Defense-in-Depth
- [Pydantic v2 Strict Mode](https://docs.pydantic.dev/latest/concepts/strict_mode/)
- SDK-005 — TypeScript-spezifische Variante
- OBS-001 — Validation-Error als Execution-Error formatieren
